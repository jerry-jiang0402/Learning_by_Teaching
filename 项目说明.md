# 聊天机器人项目

这是一个使用 **Python + FastAPI** 后端和 **Vue 3** 前端构建的聊天机器人应用。

## 🏗️ 项目架构

```
semester_project_epfl/
├── backend/                 # Python FastAPI 后端
│   └── main.py             # 主要后端服务器文件
├── frontend/               # Vue 3 前端
│   ├── src/
│   │   ├── components/     # Vue 组件
│   │   ├── views/         # 页面视图
│   │   └── router/        # 路由配置
│   ├── package.json       # Node.js 依赖
│   └── vite.config.ts     # Vite 配置
├── requirements.txt       # Python 依赖
├── start_backend.py      # 后端启动脚本
├── start_frontend.bat    # Windows 前端启动脚本
└── start_frontend.sh     # Linux/Mac 前端启动脚本
```

## 🛠️ 技术栈

### 后端 (Python + FastAPI)
- **FastAPI 0.104+** - 现代高性能 Web 框架
- **WebSockets** - 实时双向通信
- **Uvicorn** - ASGI 服务器
- **Pydantic** - 数据验证

### 前端 (Vue 3 + TypeScript)
- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Lucide Vue** - 图标库

## 🚀 快速开始

### 环境要求
- Python 3.9+
- Node.js 16+
- npm 或 yarn

### 1. 安装后端依赖
```bash
pip install -r requirements.txt
```

### 2. 启动后端服务器
```bash
python start_backend.py
```
后端将在 `http://localhost:8000` 运行

### 3. 安装前端依赖并启动
**Windows:**
```bash
start_frontend.bat
```

**Linux/Mac:**
```bash
chmod +x start_frontend.sh
./start_frontend.sh
```

前端将在 `http://localhost:5173` 运行

### 4. 访问应用
打开浏览器访问 `http://localhost:5173`

## 🌟 功能特性

### 聊天界面 (左侧)
- ✅ 实时消息发送和接收
- ✅ WebSocket 连接状态显示
- ✅ 消息时间戳
- ✅ 用户和AI消息区分
- ✅ 自动滚动到最新消息
- ✅ 正在输入指示器

### 仪表板 (右侧)
- ✅ 实时统计信息
  - 总消息数
  - 在线用户数
  - 系统运行状态
- ✅ 最近消息列表
- ✅ 系统信息面板
- ✅ 快捷操作
  - 清空聊天记录
  - 导出聊天记录

### 技术特性
- ✅ WebSocket 实时通信
- ✅ 自动重连机制
- ✅ 响应式设计
- ✅ TypeScript 类型安全
- ✅ 现代化 UI 设计

## 🔧 API 端点

### HTTP API
- `GET /` - 根路径
- `GET /api/health` - 健康检查
- `GET /api/chat/history` - 获取聊天历史
- `GET /api/dashboard/stats` - 获取仪表板统计

### WebSocket
- `WS /ws/chat` - 聊天 WebSocket 连接

## 📝 开发说明

### 后端开发
- 主要逻辑在 `backend/main.py`
- 支持 WebSocket 连接管理
- 内置消息历史记录
- 可扩展 AI 集成

### 前端开发
- 使用 Vue 3 Composition API
- TypeScript 严格模式
- Tailwind CSS 样式
- 组件化架构

## 🎨 自定义配置

### 修改端口
**后端端口 (默认8000):**
编辑 `start_backend.py` 中的 port 参数

**前端端口 (默认5173):**
编辑 `frontend/vite.config.ts` 中的 server.port

### 样式自定义
编辑 `frontend/tailwind.config.js` 来自定义主题色彩

### AI 集成
在 `backend/main.py` 的 WebSocket 处理函数中添加真实的 AI 服务调用

## 🐛 故障排除

1. **后端启动失败**
   - 检查 Python 版本 (需要 3.9+)
   - 确保已安装所有依赖: `pip install -r requirements.txt`

2. **前端启动失败**
   - 检查 Node.js 版本 (需要 16+)
   - 删除 `frontend/node_modules` 并重新安装: `npm install`

3. **WebSocket 连接失败**
   - 确保后端服务器正在运行
   - 检查防火墙设置
   - 查看浏览器控制台错误信息

## 📈 扩展建议

1. **数据库集成** - 添加 PostgreSQL 或 MongoDB
2. **用户认证** - 实现用户登录和会话管理
3. **AI 集成** - 接入 OpenAI、Claude 或其他 AI 服务
4. **消息持久化** - 将消息保存到数据库
5. **多房间支持** - 支持多个聊天室
6. **文件上传** - 支持图片和文档分享

## 📄 许可证

本项目仅用于学习和开发目的。

python start_backend.py


cd frontend
npm install
npm run dev
